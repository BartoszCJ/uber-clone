import { SafeAreaView, Text } from "react-native";
import { View } from "react-native-reanimated/lib/typescript/Animated";

const Home = () => {
  return (
    <SafeAreaView>
      <Text>Homeeeeeexedddddddddd</Text>
    </SafeAreaView>
  );
};

export default Home;
